import express from 'express';
import { MongoClient } from 'mongodb';
import { mongoServerUrl } from '../config/server'
const app = express();

(async ()=> {
    let client = null;
    let db = null;

    client = await 


})();